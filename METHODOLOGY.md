# 方法論：每一步怎麼算，以及為什麼這樣算

[← 回 README](README.md)

這份文件把整條計算鏈拆開講清楚。包含我**一開始算錯、後來才修正**的三個地方——
因為那三個錯誤本身就說明了為什麼最後的做法必須長這樣。

目錄：

1. [資料長什麼樣子](#1-資料長什麼樣子)
2. [第一步：去重（不做這步，帳單會翻倍）](#2-第一步去重)
3. [第二步：算出每個 turn 的 context](#3-第二步算出每個-turn-的-context)
4. [第三步：核心恆等式](#4-第三步核心恆等式)
5. [第四步：分段（我第一次算出 325%）](#5-第四步分段)
6. [第五步：歸因到具體事件](#6-第五步歸因到具體事件)
7. [第六步：計價](#7-第六步計價)
8. [第七步：判定哪些是浪費](#8-第七步判定哪些是浪費)
9. [第八步：反事實模擬](#9-第八步反事實模擬)
10. [誤差從哪來](#10-誤差從哪來)
11. [為什麼不用其他做法](#11-為什麼不用其他做法)
12. [即時判斷：什麼時候該重開](#12-即時判斷什麼時候該重開)

---

## 1. 資料長什麼樣子

Claude Code 把每個 session 寫成一個 `.jsonl`，放在：

```
~/.claude/projects/<專案路徑編碼>/<session-id>.jsonl
```

一行一個 JSON。`type` 欄位決定它是什麼。實測一個檔案裡的分佈：

```
assistant            3315
attachment           1962
user                 1784
bridge-session        497
mode                  495
...
```

我們要的是 `type: "assistant"`。它的 `message.usage` 長這樣：

```json
{
  "input_tokens": 2,
  "cache_creation_input_tokens": 56294,
  "cache_read_input_tokens": 0,
  "output_tokens": 515,
  "output_tokens_details": { "thinking_tokens": 372 },
  "cache_creation": {
    "ephemeral_1h_input_tokens": 56294,
    "ephemeral_5m_input_tokens": 0
  }
}
```

**`cache_creation` 這個子物件很重要。** 它把快取寫入拆成 1 小時 TTL 和 5 分鐘 TTL，
而這兩者**價格差很多**（見[第六步](#7-第六步計價)）。只看
`cache_creation_input_tokens` 會算錯錢。

---

## 2. 第一步：去重

### 問題

**同一則 assistant 訊息會被寫進 log 好幾次。**

Claude Code 是串流寫入的：每產生一個 content block（thinking、text、tool_use）
就寫一行，而**每一行都帶著完整且相同的 `message.usage`**。

實測一個檔案：

```
不重複的 message.id 數量分佈：
  出現 1 次：629 則
  出現 2 次：617 則
  出現 3 次：484 則
```

### 如果不處理會怎樣

```
逐行加總：      1,574,067,836 tokens
以 message.id 去重： 837,610,495 tokens
                    ↑ 差 1.88 倍
```

**你的帳單會被算成接近兩倍。** 這是這個領域最容易踩、後果最大的一個坑。

### 做法

以 `message.id` 為 key。同一個 id 只建立一個 turn，usage 只取一次，
但把各行的 content block **累加**起來（因為每一行只帶其中一塊，
而完整的輸出內容會影響下一個 turn 要扛多少）。

```python
if cur is None or cur.mid != mid:
    # 新的一則訊息 → 建立 turn，記錄 usage
else:
    # 同一則的後續片段 → 只累加 content，不重複計 usage
```

程式在 `loader.py:parse_session()`。
測試在 `tests/test_audit.py:TestDeduplication`。

---

## 3. 第二步：算出每個 turn 的 context

一個 turn 實際被計費的 context 總量是：

```
ctx(i) = input_tokens
       + cache_creation_input_tokens
       + cache_read_input_tokens
```

**為什麼是這三個相加？**

因為這三者就是「這次請求送進去的所有 input」的三種計價狀態：

- `cache_read` — 已經在快取裡，便宜（基礎價的 0.1 倍）
- `cache_creation` — 這次新寫進快取的，貴（1.25 或 2 倍）
- `input` — 完全沒進快取的零頭

三者加起來，就是模型這一輪「看到」的完整 context 大小。

`output_tokens` **不算**在內——它是產出，不是被扛著的東西。

---

## 4. 第三步：核心恆等式

這是整個工具的地基。

### 推導

定義每個 turn 的 context 成長量：

```
delta(i) = ctx(i) − ctx(i−1)
```

也就是「在 turn i 之前，有什麼新東西進到 context 裡」。

那麼：

```
ctx(i) = delta(0) + delta(1) + ... + delta(i)
```

而總快取讀取量是每個 turn 的 context 累加：

```
Σ ctx(i)  =  Σ over i of ( Σ over j≤i of delta(j) )
```

把求和順序交換，改成「每一筆 delta 被算了幾次」：

```
Σ 快取讀取  =  Σ  delta(i) × （它之後還有幾個 turn）
                ↑ 每一筆 delta，乘上它被重讀的次數
```

### 這代表什麼

**每一個快取讀取 token，都可以被精確歸還給某一次「把東西塞進 context」的動作。**

這不是估計，是一個恆等式。這就是為什麼這個工具能回答「為什麼」，
而按時間分桶的工具不能——它們的資料結構裡沒有「delta」和「之後還有幾個 turn」
這兩個維度。

### 實測驗證

在一個 1730 turn 的真實 session 上：

```
實際 cache_read：   827,117,190
用恆等式重建：      835,378,615
                    誤差 +1.00%
```

剩下的 1% 來自壓縮，也就是下一步要處理的事。

### 這也解釋了「乘數」

一筆內容的成本 = 它的大小 × 它之後還有幾個 turn。

所以**同樣的東西，早讀比晚讀貴得多**。
在 900 turn 的 session 裡第 5 輪讀進來的檔案，要被收費 895 次；
最後一輪讀進來的，一次都不用。

---

## 5. 第四步：分段

### 我第一次算錯的地方

我第一版直接套用恆等式，結果保真度是 **325%**——
歸因出來的金額是實際帳單的三倍多。

### 原因

**壓縮（compaction）會讓 context 崩落。**

當 Claude Code 壓縮歷史，`ctx` 會從接近 1M 掉到 20 萬。
壓縮之前的那些內容**已經不在 context 裡了，不再被計費**。

但我的第一版程式仍然讓它們「被扛到 session 結束」，
同時又把壓縮後重建的 context 當成一筆新的 delta 加進去。等於雙重計算。

### 做法

先掃一遍找出所有「context 大幅下降」的位置，把 session 切成數個**區段**：

```python
RESET_DROP = 0.80   # context 掉到前一輪的 80% 以下 → 視為重置

def segments(turns):
    bounds = [0]
    prev = 0
    for i, t in enumerate(turns):
        if prev and t.ctx < prev * RESET_DROP:
            bounds.append(i)
        prev = t.ctx
    bounds.append(len(turns))
    return list(zip(bounds, bounds[1:]))
```

然後，一筆事件的「被重讀次數」只算到**它自己那個區段的結尾**，不是整個 session：

```python
carried = end - 1 - i      # 而不是 len(turns) - 1 - i
```

### 修正後

```
保真度：325% → 100.7%
```

**為什麼門檻設 0.80？** 正常一輪的 context 只會往上長一點點，
壓縮則是斷崖式下跌（通常掉到原本的 20–30%）。0.8 這個門檻足以區分兩者，
又不會把正常波動誤判成重置。

測試在 `tests/test_audit.py:TestCompaction`。

---

## 6. 第五步：歸因到具體事件

到這裡我們知道「turn i 進來了 delta(i) 這麼多東西，被扛了 N 次」。
但**那些東西到底是什麼？**

### 收集事件

一邊逐行讀檔，一邊把兩個 assistant turn 之間發生的事累積起來：

| 事件類型 | 來源 |
|---|---|
| `tool_result` | user 記錄裡的 tool_result block ＋ 行層級的 `toolUseResult` |
| `assistant_output` | 前一個 turn 自己的輸出（thinking + text + tool_use） |
| `user_message` | 你打的字 |
| `attachment` | attachment 記錄 |
| `system` | system 記錄 |

工具名稱和目標檔案是用 `tool_use_id` 對回去的：
assistant 發出 tool_use 時記下 `id → (工具名, 目標)`，
之後 user 回傳 tool_result 時用同一個 id 查回來。

### 拆分 delta

一個 delta 可能由好幾個事件共同造成。做法是**按實測大小等比例拆分**：

```python
share = delta * event.approx // total_approx
```

`approx` 是把該事件序列化成 JSON 後的字元數 ÷ 4。

**為什麼可以用這麼粗糙的估計？**

因為只有事件**之間的比例**會影響結果，那個「÷ 4」的常數在分子分母中相消。
而且**總量被鎖定在真實的 delta 上**——不管怎麼拆，加起來一定等於實際計費的數字。

### 精確與估計的分界

| | |
|---|---|
| **精確** | 總量、每個區段的 delta、被重讀次數、所有加總 |
| **估計** | 單一筆 delta 在數個事件之間怎麼分配 |

所以工具每次都印出**歸因保真度**，讓你自己判斷可信度。

---

## 7. 第六步：計價

費率取自
[官方定價頁](https://platform.claude.com/docs/en/about-claude/pricing)（2026-09-22 擷取）。

### 每百萬 token 美元

| 模型 | 輸入 | 5m 寫入 | 1h 寫入 | 快取讀取 | 輸出 |
|---|---:|---:|---:|---:|---:|
| Opus 5 | $5 | $6.25 | $10 | **$0.50** | $25 |
| Sonnet 5 | $2 | $2.50 | $4 | **$0.20** | $10 |
| Fable 5.1 | $10 | $12.50 | $20 | **$0.25** | $50 |
| Haiku 4.5 | $1 | $1.25 | $2 | **$0.10** | $5 |

### 兩個容易算錯的地方

**1. 1 小時 TTL 的寫入是 2 倍，不是 1.25 倍**

- 5 分鐘 TTL 寫入 = 基礎輸入價 × 1.25
- **1 小時 TTL 寫入 = 基礎輸入價 × 2**

所以要讀 `cache_creation.ephemeral_1h_input_tokens` 和
`ephemeral_5m_input_tokens` 分開計價。混在一起算會低估。

**2. Fable 5.1 的快取讀取是 0.025 倍，不是 0.1 倍**

其他所有模型都是 0.1 倍，只有 Fable 5.1 / Mythos 5.1 是 **0.025 倍**。
用通則去算會高估 4 倍。

### 為什麼「快取寫入是快取讀取的 20 倍」

以 Opus 5 為例：

```
1h 寫入 $10.00 / MTok
快取讀取 $0.50 / MTok
        → 20 倍
```

這個倍數是[第八步](#9-第八步反事實模擬)那個反直覺結論的直接原因。

### 未知模型怎麼處理

找不到的模型 ID 會**退回用 Opus 5 的費率**，而不是當成 0。
理由：新模型發布時，靜默地算成免費會嚴重低估帳單；
用一個真實的費率至少數量級是對的。

`<synthetic>` 這類本地產生的 turn 則計為 0，因為它們確實不經過 API。

---

## 8. 第七步：判定哪些是浪費

「可以省」是一種**判斷**，不是事實。所以每一條規則都會把
**自己的認定方式印出來**，讓你有辦法不同意它。

### 重複讀取

同一個 session 內，同樣的「工具 + 目標」第二次以後的讀取。

- **只算唯讀工具**：Read、Grep、Glob、NotebookRead、WebFetch、Bash、PowerShell
- **排除 Edit / Write**：反覆修改同一個檔案是正常工作，不是重複
- **假設**：期間檔案沒有變動

計入的是**第二次以後那幾筆的完整攜帶成本**。

### 快取重寫

實際的快取寫入量，減掉 context 成長所能解釋的量：

```python
churn = actual_write_tokens - attributed_write_tokens
```

**為什麼會有差？** 因為快取項目會過期。session 閒置超過 TTL 後再繼續，
同樣的內容要重新寫進快取一次——而且是以**寫入價**（20 倍）。

這一項是我在對帳時發現的：帳面差了約 10%，追下去才發現是重寫。

### 過大的工具輸出

單筆工具結果超過 10k tokens（可用 `--oversized` 調整）。

**只計算超過門檻的那一部分**，而不是整筆。
理由：那個輸出裡總有一部分是你真的要的，把整筆算成浪費不誠實。
相當於假設「原本就該分頁、過濾，或加上 limit」。

### 失敗的工具呼叫

判定條件：非零結束碼、`is_error`、被中斷、stderr 有東西但 stdout 空、
錯誤字串開頭、或搜尋無結果（"No matches found" 之類）。

這類**全部攜帶成本都算浪費**——因為它什麼有用的都沒回傳，
卻一樣佔著 context 到 session 結束。

---

## 9. 第八步：反事實模擬

### 為什麼這是重播，不是猜測

我們有**實測的 delta 序列**。一個策略改變的只是
「每個 turn 還扛著哪些 delta」，費率則完全不變。

所以可以直接重跑一遍算出新帳單。唯一估計的輸入是
「重開 session 後要重建多少 context」（`--rebuild`，預設 20 個 turn），
而且這筆重建成本是**要付錢的**，不會被假裝成免費。

### 我第二次算錯的地方

第一版的基準（baseline）用的是**實測帳單**，而各策略用的是**模擬結果**。

這樣比較會把「模型本身的誤差」混進「策略的效果」裡，
算出來的節省金額不可信。

**修正**：基準也走同一條模擬程式碼，只是不設上限。

```python
base_usd = sum(cap_context(th, NO_CAP, rebuild) for th in threads)
```

這樣兩邊由**完全相同的算術**產生，差異就純粹來自策略本身。

### 我第三次算錯的地方

模擬忽略了真實發生過的壓縮，導致模擬中的 context 永遠只增不減。

```
修正前：模擬基準 $4,296  vs  實測 $2,821  → 誤差 +52%
修正後：模擬基準 $2,340  vs  實測 $2,598  → 90%（剩下的差是快取重寫）
```

**修正**：模擬也要按真實的區段邊界切開跑。

### 為什麼結論是「不要重開得太勤」

模擬出來的曲線**不是單調的**：

```
300k 換新 → 省 27%
500k 換新 → 省 22%
200k 換新 → 省 10%
150k 換新 → 多花 25%
100k 換新 → 多花 236%
```

原因就是[第六步](#7-第六步計價)那個 20 倍：
每次重開都要把常駐 context（實測中位數約 54k tokens）以**寫入價**重新買一次。
重開越頻繁，這筆固定成本被付的次數越多。

到某個點，省下的重讀費用就被重建費用吃光了。**最佳點大約在 300k。**

這也是為什麼這個工具不會給你「清空 context 就對了」這種建議——
資料不支持。

---

## 10. 誤差從哪來

工具會主動印出自己的誤差，不藏。

| 來源 | 大小 | 說明 |
|---|---|---|
| 歸因保真度 | 99–101% | 單筆 delta 在事件間的比例拆分是估計 |
| 模擬 vs 實測 | 約 90% | 模擬不重現快取過期造成的重寫 |
| token 估計 | 相消 | 4 字元／token，只影響比例不影響總量 |

**還有兩件事是 log 本身做不到的：**

1. **turn-0 那包拆不開。** system prompt 不會被寫進 log，所以無法分出
   MCP schema / CLAUDE.md / system prompt 各佔多少。
   `baseline` 精確量測整包，但明講它拆不開。
   想看出差別，可以比較連了不同 MCP server 的 session
   （實測範圍是 23,446 到 67,108 tokens）。

2. **金額不等於你付的錢。** 如果你用 Pro / Max 訂閱，這些是 API 牌價換算值。
   請看相對大小，不要看絕對金額。

---

## 11. 為什麼不用其他做法

### 為什麼不直接數 tool 輸出的大小

那會算出「這次讀檔多大」，而不是「這次讀檔花了多少錢」。
兩者差距可以到幾十倍——取決於它落在 session 的哪個位置。
不乘上重讀次數，就看不到真正的成本結構。

### 為什麼不按時間分桶

那就是 ccusage 在做的事，而且它做得很好。
但「每天花多少」推不出「哪個動作造成的」，資料模型裡沒有那個維度。

### 為什麼不用 token 計算 API 去精算每個事件

要為幾十萬個事件各打一次 API，慢、要錢、而且**沒必要**——
因為總量已經被真實計費數字鎖死了，我們只需要事件之間的**相對比例**，
字元數就足夠，且誤差會相消。

### 為什麼不在執行時即時攔截

那是另一種產品（例如 governor 那類做 context 過濾的工具），解決的是「預防」。
這個工具解決的是「**已經花掉的錢，到底花在哪**」——
一個是事前，一個是事後，兩者不互相取代。

---

## 12. 即時判斷：什麼時候該重開

前面十一節都是事後分析。但你真正需要做決定的時刻，是在 session 進行中。

### 為什麼「context 到 X% 就清掉」是錯的形狀

固定門檻沒有考慮**你的開場有多重**。
如果你的常駐 context 是 20k，跟 67k 的人，最佳時機差很多。

實測你的 turn-0 中位數是 54k，範圍 23,446 到 67,108——差了快三倍。

### 正確的問法

不是「context 大不大」，而是「**現在重開，划得來嗎**」。

三個成本：

```
繼續扛     每輪  ctx  × 讀取價
重開之後   每輪  base × 讀取價
重開本身   一次  base × 寫入價
```

所以重開的回本輪數是：

```
T = base × 寫入價 / ((ctx − base) × 讀取價)
```

因為 1 小時 TTL 的寫入價是讀取價的 20 倍，可以簡化成：

```
T ≈ 20 × base / (ctx − base)
```

### 代入實際數字（base = 54k）

| 現在的 context | 每輪成本 | 重開後每輪 | 回本需要 |
|---:|---:|---:|---:|
| 100,000 | $0.050 | $0.027 | 23.5 輪 |
| 150,000 | $0.075 | $0.027 | 11.2 輪 |
| 200,000 | $0.100 | $0.027 | 7.4 輪 |
| 300,000 | $0.150 | $0.027 | **4.4 輪** |
| 500,000 | $0.250 | $0.027 | 2.4 輪 |
| 999,000 | $0.500 | $0.027 | 1.1 輪 |

這條曲線也解釋了為什麼[第八步](#9-第八步反事實模擬)模擬出來的最佳點在 300k 附近：
再往下，回本輪數就長到「還沒回本就又要重開了」。

### 掛成狀態列

```json
{ "statusLine": { "type": "command", "command": "cc-token-audit statusline" } }
```

Claude Code 會把 session 的 JSON 從 stdin 餵進來，包含
`context_window.used_percentage`（它的算法是
`input + cache_creation + cache_read`，**跟本文第二步的 ctx 完全同一個公式**）、
以及 `prompt_cache.expires_at` 和 `recache_tokens_if_cold`。

實作上有兩個限制要遵守：

1. **要快。** 它每次畫面更新都會跑，所以不解析整個 transcript，
   只讀到第一筆 assistant 記錄就停（拿 base）。實測約 130ms。
2. **絕不能崩。** 任何例外都降級成空字串，
   不能讓一個 traceback 卡在使用者的 UI 底部。

### /compact 還是 /clear

工具算得出成本，但算不出你的意圖，所以這條要你自己判斷：

- **換一件不相干的事** → `/clear`。舊脈絡完全不需要，只重付開場那包，最便宜。
- **同一件事要接著做** → `/compact`。摘要保留脈絡，但摘要本身也是一筆要付寫入價的
  context——實測資料裡壓縮後的重建（`context_rebuild`）就是單筆最貴的幾個項目
  （$23.63、$13.19、$10.59），所以不要太頻繁。
- **回本輪數還很大** → 都別動，現在動反而虧。

---

[← 回 README](README.md)
